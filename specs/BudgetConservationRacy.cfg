\* Counterexample configuration: concurrent admissions deciding from stale
\* snapshots overspend the envelope. `make spec-counterexamples` asserts
\* this configuration FAILS.
SPECIFICATION Spec
CONSTANTS
    Capacity = 4
    Request = 2
    Runs = {r1, r2, r3}
    Serialized = FALSE
INVARIANT NoOverspend

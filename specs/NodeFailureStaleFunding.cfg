\* Expected consequence of the deliberate HandleNodeFailure pass-2 snapshot:
\* B's first failed active closes, promoting B's healthy exact-slot squatter
\* under a fresh derivation. A's later swap still uses the frozen post-pass-1
\* map, reclaims B, and violates PostClosureFundedWorkSurvives.
\*
\* This is an expected counterexample to the stronger current-world property,
\* not a desired-property failure and not authorization to re-evaluate inside
\* the loop. Fresh per-iteration evaluation makes the answer depend on lease
\* order and can decline A's entry-funded swap.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
  Capacity = 4
  RequireFence = TRUE
  SlotGranularReclaim = TRUE
  SparePassFirst = TRUE
  ReleaseSpareOnDecline = TRUE
  TrackedPhases = TRUE
  EvictBothPlanes = TRUE
  TopUpSpares = FALSE
  CountBasedTopUp = FALSE
  ConsumedSpareByName = TRUE
  ExternalDeletes = FALSE
  FreezeFundingForPass2 = TRUE

INVARIANTS
  TypeOK
  PostClosureFundedWorkSurvives

\* Two independent attempts observe the one remaining n1 slot before either is
\* assumed. Without the scheduler cache's atomic AssumePod revalidation, both
\* commit from the stale observation and later bind beside Old: 3 physical GPU
\* requests on capacity 2.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
  InitMode = "Lifecycle"
  AtomicCapacityCommit = FALSE
  ClosedLeaseFreesPhysical = FALSE
  MaterializationGuard = TRUE
  SwapChecksCapacity = TRUE
  IdempotentPreBind = TRUE
  RetryPlacementGuard = TRUE
  RequireBindFailureCleanup = TRUE
  MaxSteps = 14

INVARIANT PhysicalCapacitySafe

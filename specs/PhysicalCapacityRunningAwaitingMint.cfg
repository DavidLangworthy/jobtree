\* Legal witness rejected by pkg/invariant: HandleNodeFailure has closed the old
\* active/spare leases and emitted a replacement pod, but scheduler PreBind has
\* not yet minted the replacement. Run phase remains Running.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
  InitMode = "RunningAwaitingMint"
  AtomicCapacityCommit = TRUE
  ClosedLeaseFreesPhysical = FALSE
  MaterializationGuard = TRUE
  SwapChecksCapacity = TRUE
  IdempotentPreBind = TRUE
  RetryPlacementGuard = TRUE
  RequireBindFailureCleanup = TRUE
  MaxSteps = 14

INVARIANTS
  TypeOK
  MachinePlaneConsistent
  PhysicalCapacitySafe
  OneMachineLivePodPerRank
  RunningImpliesOpenActiveLease

# Jobtree concepts

This repository implements the Budget, Run, Reservation, and Lease custom resources that underpin the GPU/HPC scheduler described in the golden-thread document. The goal of this initial milestone is to provide type-safe API definitions, validation, and samples that match the specification so later controllers can build on a solid foundation.

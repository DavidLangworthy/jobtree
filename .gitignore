bin/
dist/
specs/.cache/
_apalache-out/
specs/states/

# Claude Code local state (workflows/ IS tracked and shared)
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
.autopilot-last.log

# Per-run autopilot scratch log; never belongs in a PR branch.
.autopilot-turn.log

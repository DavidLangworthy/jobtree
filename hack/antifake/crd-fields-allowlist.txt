# Anti-fake lint allowlist: api/v1 CRD fields with zero non-generated,
# non-test readers outside api/v1 (Track F / TESTINFRA-6,
# docs/project/make-it-real-plan.md; docs/project/fake-features-audit.md §3.4
# pattern P4, "API accepts fields no controller reads").
#
# Format: one entry per line, `<StructName.FieldName> <reason>`.
# Blank lines and '#' comments are ignored.
#
# Shrink-only: a field that gains a real reader (or is deleted) must have its
# entry removed here, not left behind — hack/antifake/crdfields_test.go fails
# on stale entries. The ratchet is enforced in code:
# hack/antifake/crdfields.go's maxAllowedUnreadCRDFields caps this file at 8
# entries; raising it requires editing that constant deliberately.
#
# Three of the eight below were already named by the fake-features audit;
# the other five were newly discovered by this lint's first run and are not
# fixed by Track F (TESTINFRA) — they're recorded here as an honest,
# ratcheted baseline for whichever track (mostly D/TRUTH) triages them next.

GPULeaseSpec.CompPath reason: declared and deep-copied but written and read by zero code today (per-role GPU-hour attribution has no per-role concept yet). make-it-real-plan.md Track E (ROLES-7) populates it at mint once per-role gangs land; do not add a consumer here without that context.
ReservationStatus.CanceledAt reason: newly discovered — declared alongside ActivatedAt/ReleasedAt, which ARE written by controllers/run_controller.go; CanceledAt alone has no writer or reader, implying no cancellation flow is actually wired. Track D triage: wire a real Cancel path or delete the field.

GPULeaseSpec.PaidByPrincipal reason: DESIGN-v5 build item 5, added ahead of its consumer as a batched CRD change. It is the join INV-SUBTREE-CONSERVE needs — a lease names its payer by namespace NAME while the snapshot keys principals by namespace UID — so its reader is build item 8, subtree conservation, which does not exist yet (nothing aggregates descendants today, evaluate.go). The producer (build item 3) is what will have a principal identity to stamp. Delete this entry when conservation lands; do NOT wire a consumer that re-derives the principal from names, which is the exact unsoundness the field exists to avoid.
GPULeaseSpec.SnapshotVersion reason: DESIGN-v5 build item 5, same batched CRD change. Its ONLY justification is in-flight mint verification (a mint is not atomic with respect to the producer, so a lease must record which accepted snapshot authorised it), and the producer that publishes those versions is build item 3. It is NOT an accrual anchor — v4 claimed that and Ruling 10 deleted the accrual — so anyone triaging this entry by asking what it anchors will reach the wrong answer. Delete this entry when the producer and loader land.

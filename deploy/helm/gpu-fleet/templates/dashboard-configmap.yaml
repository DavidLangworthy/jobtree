{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gpu-fleet.name" . }}-dashboards
  labels:
    grafana_dashboard: "1"
    {{- include "gpu-fleet.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "dashboards/*.json").AsConfig | indent 2 }}
{{- end }}
